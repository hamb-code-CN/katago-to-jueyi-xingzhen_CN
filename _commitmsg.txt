fix(ci): 发版流水线两处硬伤 + 发版说明自动取自 README 更新日志

- ci.yml 语法检查: python -m py_compile go_ai/*.py 在 pwsh 下通配符不展开,
  字面量被当文件名 → [Errno 22] 直接中断。改用 compileall 且该步切到 bash。
- tools/make_release.py: runner 控制台是 cp1252, 打印中文抛 UnicodeEncodeError
  导致 job 失败。启动时强制 stdout/stderr 为 UTF-8。
- 新增 tools/release_notes.py: 从 README 中英更新日志抽取当前版本那一节生成
  Release 正文, 发版说明与仓库文档不再各自漂移; release.yml 改用它。
- 新增 tests/test_release_notes.py 7 例 (含 v1.0.3 不能误匹配 v1.0.30 的回归锁)。
- 文档: 用例数 121, 补充 CI 修复与发版说明生成。

测试: 121 例全过。
