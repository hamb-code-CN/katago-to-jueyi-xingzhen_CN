# Python 依赖 (Python 3.10 ~ 3.12)
# 版本锁定: 便携包在用户机器上必须可复现。历史上出现过 cv2 大版本漂移导致
# "中文路径 imwrite 静默失败" 这类问题, 所以这里给出**实测可用**的精确版本。
# 如需在更新的机器上装, 先按 requirements-flexible.txt (>=) 装再回归测试。
opencv-python==5.0.0.93
numpy==2.2.6
pillow==12.3.0
PyAutoGUI==0.9.54
rapidocr-onnxruntime==1.4.4
