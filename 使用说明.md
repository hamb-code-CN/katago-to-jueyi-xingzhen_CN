# 围棋 AI 自动对局系统 — 便携部署包

在任意一台 Windows 电脑上，解压本包即可运行（无需改任何代码路径）。

## 包结构

```
围棋AI便携包/
├─ go_ai/                 全部 Python 代码 + 网页看板 + 配置 + 架构文档
├─ katago/                KataGo 引擎 (opencl171/katago.exe + dll) + 模型 + gtp.cfg
├─ requirements.txt       Python 依赖清单
├─ 启动围棋AI.bat         一键启动: 首次自动建 venv 并装依赖
└─ 使用说明.md            本文件
```

## 新电脑部署（约 3~5 分钟）

### 前提

1. 安装 **Python 3.10 ~ 3.12**（https://www.python.org/downloads/），
   安装时务必勾选 **“Add Python to PATH”**。
2. 首次联网安装依赖（只需一次）。

### 步骤

```
1. 把 zip 解压到任意目录（建议无中文的路径，如 D:\GoAI）
2. 双击「启动围棋AI.bat」
   - 首次运行: 自动创建 venv 并 pip 安装 requirements.txt（等待完成）
   - 之后运行: 直接启动，并自动打开浏览器看板
3. 浏览器进入 http://127.0.0.1:8123/analysis.html
4. 看板「对手软件」下拉选择 腾讯围棋 / 星阵围棋
   → 选择 智力档位 → 选择 **落子方式**（见下）→ 选择 执黑/执白 → 点「启动 AI」
5. 需要落子方式时看板中修改：`AI 自动落子` / `辅助模式` / `仅分析`
```

### 三种落子方式（v1.0.3）

| 落子方式 | 谁决定落哪 | 适合场景 |
| --- | --- | --- |
| **AI 自动落子** | AI 自己下 | 全自动托管 |
| **辅助模式** | **你点选**（AI 只给胜率/候选） | 想自己下、但要看 AI 的胜率与推荐点 |
| **仅分析** | 谁都不下 | 纯围观、研究局面 |

**辅助模式怎么用**：启动 AI 时落子方式选「辅助模式」，然后

- 在数字棋盘上**点任意交叉点** → AI 在客户端帮你把这一手落下去；
- 或直接**点右侧候选行的任意一条** → 落到 AI 推荐的那个点。

点完会有提示；如果你点的位置**已有子 / 是劫 / 会自杀 / 是禁手**，会被拦下并告诉你原因，不会把客户端点乱。
落子点超过 20 秒没执行会自动作废（棋盘可能已经变了）。

### 手动启动（不用 bat）

```bat
venv\Scripts\python.exe go_ai\analysis_watch.py      :: 看板后端 (:8123)
venv\Scripts\python.exe go_ai\go_launcher.py          :: 或命令行菜单启动器
```

### 命令行直接跑 AI

```bat
venv\Scripts\python.exe go_ai\go_controller.py --color black --tmin 1.5 --tmax 3.0 --interval 5 --platform tencent
venv\Scripts\python.exe go_ai\go_controller.py --color white --platform xingzhen
venv\Scripts\python.exe go_ai\go_controller.py --color black --platform xingzhen --assist      :: 辅助模式
```

> 所有参数都可省略：省略时读 `go_ai/settings.json`（首次运行自动从旧的
> `launcher_config.json` 迁移）。加 `--no-auto-stop` 可关闭"终局自动停止"。

## 一局结束后会发生什么（v1.0.3）

识别到终局（引擎认输 / 连续虚着 / 盘面长时间静止且界面已无行棋印章）时，AI 会：

1. 导出棋谱到 `go_ai/games/game_YYYYmmdd_HHMMSS.sgf`（带真实落子顺序，可直接复盘）
2. 把结果写进 `go_ai/game_result.json`（看板显示"上一局"）
3. 按需发通知（`settings.json → notify`，默认关闭）
4. 自动停止自己

想手动留档：看板「启动器 → 棋谱 / 终局 → 导出当前棋谱」。

## 自愈能力（v1.0.3）

- **KataGo 服务**：内置看门狗，引擎进程崩溃后每 10s 自动重建（日志 `go_ai/kata_service.log`）
- **controller**：异常退出后由看板看门狗自动拉起；你主动点"停止"或终局自动退出都不会被误拉起
- **单实例保护**（v1.0.3/v1.0.3）：同一台机器只允许一个 AI 占棋盘。若提示「已有 controller 在运行」，
  说明上一个还没停干净 —— 看板点「停止 AI」即可。**同一台机器不要开两个看板。**

> ⚠️ **改了代码（或更新了版本）一定要重启看板**。看板是把代码加载进内存运行的，
> 只换磁盘上的文件、不重启进程，跑的还是旧逻辑。

## 看板：数字棋盘 + 后台识别（v1.0.3）

- **数字棋盘**：看板默认用前端画出来的棋盘（木纹底、渐变棋子、中文坐标、候选点胜率标签、
  最后一手红点），比截图干净得多。顶栏「数字棋盘 / 实拍」可随时切换，选择会记住。
- **后台识别**：读盘默认用 PrintWindow 直接取窗口内容，**围棋窗口被盖住、甚至最小化也能识别**，
  不用一直把棋盘摆在最前面。看板「启动器 → 取图方式」里能看到当前来源（窗口 / 全屏）。

> ⚠️ 注意：**自动落子仍然需要围棋窗口在前台** —— 落子是模拟鼠标点击，窗口被别的程序挡住时
> 点击会落到别人身上。后台取图解决的是"识别/看板不受遮挡影响"，不是"后台也能下棋"。

- **数字棋盘左右反了？** 说明该客户端的窗口图是镜像的：看板「取图方式 → 水平镜像」点一下即可，
  也可以直接改 `go_ai/settings.json` 里的 `capture.mirror`（`"auto"` / `"on"` / `"off"`）。

## 注意事项

- **显卡**：NVIDIA 显卡首次运行 KataGo 会自动做 OpenCL 调优（1~2 分钟，生成缓存到
  `katago\opencl171\KataGoData\`）；Intel/AMD 集成显卡也能跑（稍慢）。
- **打劫**：修复已内置（见架构文档 6.5 节）。请让 AI **从一局新棋开局启动**，
  这样它能记录完整落子历史，打劫处理最完整。
- **路径自动识别**：代码会自动在 `go_ai/../katago` 找引擎；若你把 katago 放到别的
  位置，改动 `go_ai/katago_engine.py` 顶部 `_find_katago_root()` 的候选路径即可。
- **打包范围**：zip 已排除运行日志(*.log)、截图调试图(*.png)、__pycache__ 等
  运行期生成物；这些会在运行时自动重建。
- **依赖版本**：`requirements.txt` 已锁定精确版本（换新机器可用
  `requirements-flexible.txt`，装完请跑一遍测试）。
- **自测**：
  ```bat
  cd go_ai && venv\Scripts\python.exe -m unittest discover -s tests -v
  ```
  50 个用例（规则/劫/识别/配置/棋谱/终局判定/主循环），约 2 秒，不需要屏幕和显卡。
  另有历史脚本 `test_ko.py`（劫判定 21 项）、`test_ko_integration.py`（12 项）。

## 架构文档

完整架构、运行逻辑、实测数据见 `go_ai/GO_AI_ARCHITECTURE.md`。
